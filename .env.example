# docker-compose already sets these automatically for local grading

# MySQL database connection
DB_HOST=db
DB_USER=writeituser
DB_PASSWORD=writeitpassword
DB_NAME=writeit

# JWT secret for session tokens (change in production)
JWT_SECRET=super-secret-key-for-grading

# Directory where uploaded images are stored (inside Docker container)
UPLOADS_DIR=/app/uploads
